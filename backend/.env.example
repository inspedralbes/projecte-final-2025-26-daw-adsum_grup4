# Configuración del Servidor
PORT=3000

# Base de Datos (MySQL)
# EN PRODUCCIÓN: Estos valores DEBEN ser sobrescritos por variables de entorno seguras o secrets
DB_HOST=db
DB_PORT=3306
DB_USERNAME=adsum_user
DB_PASSWORD=cambiar_por_contrasena_segura_en_produccion
DB_NAME=projecte_final

# Seguridad
# EN PRODUCCIÓN: Este secreto DEBE ser cambiado por un valor seguro y único
# Ejemplo de valor seguro: openssl rand -base64 32
JWT_SECRET=cambiame_por_algo_seguro
FRONTEND_URL=http://localhost:5173
